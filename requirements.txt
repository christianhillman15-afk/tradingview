# Core engine (indicators, strategies, risk, backtester, paper broker,
# synthetic data, dashboard) needs NOTHING beyond the Python standard library.
# PyYAML is only needed to read YAML config files.
PyYAML>=6.0

# --- Optional: machine-learning strategy (ml_ensemble) ---
# Install these to train/run the AI ensemble strategy.
numpy>=1.24
scikit-learn>=1.3

# --- Optional: live execution against Interactive Brokers ---
# Only needed if you wire up broker/ib.py to a real (or IB paper) account.
# Requires TWS or IB Gateway running with the API enabled.
# ib_insync>=0.9.86
