"""Optional machine-learning layer (feature engineering + ensemble model).

These modules import numpy / scikit-learn lazily so the rest of the bot keeps
running with zero third-party dependencies. Install extras with::

    pip install -r requirements.txt   # or: pip install numpy scikit-learn
"""
